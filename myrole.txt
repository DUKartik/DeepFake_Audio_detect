Developer 1: Ingress, Egress & InfrastructureYour Role: You manage the boundaries. You receive the message from Meta, enforce limits, and send the final verdict back to the user.Your Allowed Folders: webhook/, client/, utils/, and config.py. Do not let your AI generate or edit any code related to ML models or Celery workers.Core Responsibilities:FastAPI Webhook (webhook/server.py): Build the GET /webhook endpoint for Meta's verification challenge and the POST /webhook endpoint to receive incoming messages. You must validate the HMAC-SHA256 signature using the App Secret.WhatsApp Client (client/whatsapp.py): Build the class to interact with Meta Graph API v18. It needs methods to get the media download URL and send the final text message back to the sender.Rate Limiting (utils/rate_limiter.py): Implement a Redis-backed rate limiter that caps users at 10 analyses per day. You must hash the sender's phone number with SHA-256 before logging or tracking.The Handoff: Once you validate the incoming audio messager 2's asynchronous queue: pe, your job ends by triggering Developrocess_audio_task.delay(media_id, sender).